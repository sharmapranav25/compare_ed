# Secrets / env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Pipeline artifacts — never committed
# (inline comments aren't supported in .gitignore — keep comments on their own lines)
# vendor PDFs + per-doc sidecars (catalog IP)
files/
# generated outputs
BUYSHEET_*.xlsx
BUYSHEET_*.report.md
# Office lock files
~$*
*.v2.cards.json
*.v2.photo_bboxes.json
*.docling*.json
*.augmented.json
*.reconciled.*
*.skus.json
*.fields_candidates.json
*.relational.json
*.deterministic.relational.json
*.correctness.json
*.elements.json
*.normalized.json
*.layout.json
*.pictures*.json
*.pictures.csv
*.picture_index.json
*.sku_photos.json
*.sku_images.json
*.sku_images.log
*.audit/

# Logs
*.log
/tmp/

# Local notes
NOTES.md
SCRATCH.md
TODO.md

# Allow the template itself (immutable shared asset)
!BUYSHEET_template.xlsx

# YOLO weights (auto-downloaded, ~25MB binary, not committed)
buysheet_v2/models/*.pt
*.pt
